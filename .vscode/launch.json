{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Nodemon",
      "restart": true,
      "processId": "${command:PickProcess}"
    }
  ]
}
